---
id: rule_morning_briefing_unread_first
name: 'Opening gate: unread mail, then configured sites'
---
Friction 842 confirms the `reads_morning_sources_before_assembling` stall remains genuine across the latest evaluated `morning_briefing` window: the target pass read `read_morning_briefing_sites` but still omitted `gmail_user_search_threads`, and the two prior evaluated passes reached browser-site-check activity without either recurring source read. Treat both shapes as interrupted source coverage, not partial completion. If the configured site list is visible while unread Gmail is absent and unblocked, the immediate next visible move must be `read_user_email` reaching `gmail_user_search_threads` for unread mail, or a concrete Gmail auth/access/data/material blocker that explicitly limits the briefing. If browser or one-off site work appears before both recurring rows are present or blocked, stop downstream source interpretation, live snapshots, action/watch/routine grouping, final answer, handoff, and closure until the missing recurring source read(s) are backfilled or their exact blockers are recorded.

Friction 847 confirms the same `morning_briefing` quality-affordance coverage concern remains genuine in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and its three-enactment evaluation window: the latest pass read the configured site list but still omitted `gmail_user_search_threads`, and the prior two evaluated passes recorded only site-check activity without either recurring source read. For future morning-briefing passes, a site-list row without unread Gmail is an interrupted state whose only next visible source move is `read_user_email` reaching `gmail_user_search_threads` for unread mail, or a concrete Gmail auth/access/data/material blocker that explicitly limits the briefing. Browser site checks, invalid-URL handling, live snapshots, source interpretation, action/watch/routine grouping, final answers, handoffs, and closure remain downstream only after unread Gmail is present or blocked and the configured site list is present or blocked; they do not turn site-list-only or site-check-only work into a grounded briefing.

Friction 856 confirms the source-coverage gate is still a live `morning_briefing` practice-quality concern for target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and the same three-enactment window: the latest pass read `read_morning_briefing_sites` while omitting `gmail_user_search_threads`, and the two prior passes reached browser/site-check activity without both recurring source reads. Treat a configured-site-list row without unread Gmail, or any browser/site-check row before both recurring source reads, as an interrupted briefing rather than acceptable periodic variation. The immediate next visible move must be `read_user_email` reaching `gmail_user_search_threads` for unread mail, `read_morning_site_list` reaching `read_morning_briefing_sites` for configured sites when that row is also missing, or a concrete Gmail/site-list auth, access, configuration, data, or material blocker that explicitly limits the briefing; do not proceed to source interpretation, live market snapshots, action/watch/routine grouping, assembly, final answer, handoff, or closure while either recurring source remains absent and unblocked.

Friction 865 confirms this is still not just missing prose: in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66`, the configured site-list row appeared while `gmail_user_search_threads` was still absent, and the two adjacent evaluated passes reached site-check activity without both recurring source rows. For future morning-briefing passes, a site-list-only row is an unfinished gate, not a partial source baseline. The next visible move after such a row must be `read_user_email` reaching `gmail_user_search_threads` for unread mail, or a concrete Gmail auth/access/data/material blocker that says the briefing is proceeding without unread-mail grounding. If browser/site-check activity appears before both recurring source rows are present or blocked, the pass must backfill the exact missing row(s) before any live market snapshot, source interpretation, action/watch/routine grouping, assembly, final answer, handoff, or closure; downstream work must not be used as evidence that the missing recurring-source gate was satisfied.
