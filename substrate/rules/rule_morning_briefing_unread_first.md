---
id: rule_morning_briefing_unread_first
name: 'Opening gate: unread mail, then configured sites'
---
Friction 1030 confirms the `morning_briefing` source-baseline quality concern remains live across the evaluated three-enactment window: the target pass reached `read_morning_briefing_sites` while omitting `gmail_user_search_threads`, and the two adjacent passes recorded site-check activity without unread Gmail or the configured site-list read. For future morning briefings, establish the source baseline before briefing work begins: first `read_user_email` / `gmail_user_search_threads` for unread mail, then `read_morning_site_list` / `read_morning_briefing_sites` for configured recurring sites, unless the exact missing row has a concrete Gmail/site-list auth, access, account, configuration, data, or material blocker. A site-list-only start is interrupted, not partially grounded; the immediate next visible move must be unread Gmail or its blocker, and no browser/site check, live market snapshot, source interpretation, action grouping, assembly, handoff, final answer, closure, or rationale from remembered context may proceed while either baseline row remains absent and unblocked.

Friction 1062 confirms the same source-coverage stall persists in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and its evaluated three-enactment window: unread Gmail was absent from every pass, and the configured recurring-site list was absent from two passes. For future `morning_briefing` passes, treat the ordered two-row baseline as the first work product of the briefing, not a source preference to recover later. If `gmail_user_search_threads` is absent and unblocked, the immediate next visible move must be `read_user_email` reaching that material or a concrete Gmail auth/access/account/configuration/data/query/material blocker; only after that row is visible or blocked may the pass proceed to `read_morning_site_list` / `read_morning_briefing_sites` or its exact blocker. Browser checks, invalid URL handling, live market snapshots, source interpretation, assembly, handoff, final answer, verification, closure, or rationale from remembered context must not continue while either recurring source row remains unread and unblocked.

Friction 1066 confirms the same `reads_morning_sources_before_assembling` coverage failure remains live in the evaluated `morning_briefing` window: target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` reached the configured site-list row without unread Gmail, and the two adjacent passes reached site-check activity without either recurring source row. For future `morning_briefing` passes, a site-list-only row is an interrupted opening, not evidence that source gathering is underway. The immediate next visible move must be `read_user_email` reaching `gmail_user_search_threads`, or a concrete Gmail auth, access, account, configuration, data, query, or material blocker; only after that row is visible or blocked may the configured-site row or its blocker satisfy the second baseline row. Do not proceed to browser/site checks, live market snapshots, source interpretation, action grouping, assembly, handoff, final answer, verification, closure, or rationale from remembered context while either recurring source row remains absent and unblocked.
