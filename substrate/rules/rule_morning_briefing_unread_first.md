---
id: rule_morning_briefing_unread_first
name: 'Opening gate: unread mail, then configured sites'
---
Friction 1030 confirms the `morning_briefing` source-baseline quality concern remains live across the evaluated three-enactment window: the target pass reached `read_morning_briefing_sites` while omitting `gmail_user_search_threads`, and the two adjacent passes recorded site-check activity without unread Gmail or the configured site-list read. For future morning briefings, establish the source baseline before briefing work begins: first `read_user_email` / `gmail_user_search_threads` for unread mail, then `read_morning_site_list` / `read_morning_briefing_sites` for configured recurring sites, unless the exact missing row has a concrete Gmail/site-list auth, access, account, configuration, data, or material blocker. A site-list-only start is interrupted, not partially grounded; the immediate next visible move must be unread Gmail or its blocker, and no browser/site check, live market snapshot, source interpretation, action grouping, assembly, handoff, final answer, closure, or rationale from remembered context may proceed while either baseline row remains absent and unblocked.

Friction 1062 confirms the same source-coverage stall persists in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and its evaluated three-enactment window: unread Gmail was absent from every pass, and the configured recurring-site list was absent from two passes. For future `morning_briefing` passes, treat the ordered two-row baseline as the first work product of the briefing, not a source preference to recover later. If `gmail_user_search_threads` is absent and unblocked, the immediate next visible move must be `read_user_email` reaching that material or a concrete Gmail auth/access/account/configuration/data/query/material blocker; only after that row is visible or blocked may the pass proceed to `read_morning_site_list` / `read_morning_briefing_sites` or its exact blocker. Browser checks, invalid URL handling, live market snapshots, source interpretation, assembly, handoff, final answer, verification, closure, or rationale from remembered context must not continue while either recurring source row remains unread and unblocked.

Friction 1066 confirms the same `reads_morning_sources_before_assembling` coverage failure remains live in the evaluated `morning_briefing` window: target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` reached the configured site-list row without unread Gmail, and the two adjacent passes reached site-check activity without either recurring source row. For future `morning_briefing` passes, a site-list-only row is an interrupted opening, not evidence that source gathering is underway. The immediate next visible move must be `read_user_email` reaching `gmail_user_search_threads`, or a concrete Gmail auth, access, account, configuration, data, query, or material blocker; only after that row is visible or blocked may the configured-site row or its blocker satisfy the second baseline row. Do not proceed to browser/site checks, live market snapshots, source interpretation, action grouping, assembly, handoff, final answer, verification, closure, or rationale from remembered context while either recurring source row remains absent and unblocked.

Friction 1069 confirms the same opening-source failure remains live after the rule already named the ordered baseline: in the evaluated three-pass window, no pass read unread Gmail, and two passes began browser/site-check work without the configured site-list row or a concrete source blocker. For future `morning_briefing` passes, treat the first selected briefing surface as the interruption point unless the unread-Gmail row is already visible or concretely blocked. The immediate next visible move must be `read_user_email` / `gmail_user_search_threads` for unread mail, then `read_morning_site_list` / `read_morning_briefing_sites` for configured recurring sites, or the exact Gmail/site-list auth, access, account, configuration, data, query, or material blocker. Do not continue to browser/site checks, live market snapshots, source interpretation, action grouping, assembly, handoff, final answer, verification, closure, or rationale from remembered context while either baseline row remains absent and unblocked.

Friction 1073 confirms the same opening-source baseline remains a genuine `morning_briefing` delivery stall rather than acceptable variation: in the evaluated three-enactment window, unread Gmail was absent in all three runs and the configured usual-site list was absent in two of three, including target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66`, which reached the site-list row without prior unread Gmail. For future `morning_briefing` passes, the first briefing work product must be the source-baseline ledger itself, in order: `read_user_email` / `gmail_user_search_threads` for unread mail, then `read_morning_site_list` / `read_morning_briefing_sites` for configured sites, or the concrete Gmail/site-list auth, access, account, configuration, data, query, or material blocker for the exact missing row. If site-list, browser/site checking, market snapshot, source interpretation, action grouping, assembly, final answer, verification, handoff, or closure appears before the ordered baseline is visible or blocked, the pass is interrupted and the only valid continuation is the missing baseline row or its blocker; do not summarize around the absent source row from remembered morning context or one successful source read.

Friction 1079 confirms the same `reads_morning_sources_before_assembling` quality failure remains genuine in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and its evaluated three-enactment window: unread Gmail was absent in every pass, and two passes also lacked the configured recurring-site list before site-check activity. For future `morning_briefing` passes, the source-baseline ledger is the opening work product, not a recoverable omission: first invoke `read_user_email` / `gmail_user_search_threads` for unread mail or record the exact Gmail auth, access, account, query, data, configuration, or material blocker; only then invoke `read_morning_site_list` / `read_morning_briefing_sites` or record its exact blocker. If the configured site list appears before unread Gmail, or if browser/site checks, invalid URL handling, live market snapshots, source interpretation, assembly, final answer, verification, handoff, closure, or rationale appear before both source rows are visible or blocked, the pass is interrupted and the only valid continuation is the missing baseline read or its blocker.

Friction 1082 confirms the source-baseline failure still occurs in the evaluated three-pass window even when the bundle description and prior rules name the ordered reads: unread Gmail was absent in all three measured passes, and two passes also lacked the configured recurring-site-list row before site-check or invalid-URL site activity. For future `morning_briefing` passes, the first visible work product must be the ordered source-baseline ledger itself, not a site-list-only start, browser/site check, live snapshot, or remembered morning context. If `gmail_user_search_threads` is absent and unblocked, the only valid continuation is `read_user_email` reaching that material or a concrete Gmail auth, access, account, configuration, data, query, or material blocker; only after that row is visible or blocked may the pass produce `read_morning_site_list` / `read_morning_briefing_sites` or its exact blocker. Do not proceed to browser checks, invalid-URL handling, live market snapshots, source interpretation, action grouping, assembly, handoff, final answer, verification, closure, or rationale while either recurring source row remains absent and unblocked.

Friction 1086 confirms the same `reads_morning_sources_before_assembling` quality concern remains genuine in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and its evaluated three-enactment window: unread Gmail was absent in all three passes, and the configured recurring site-list material was absent in two passes. For future `morning_briefing` passes, the first visible work product is not site checking, market context, synthesis, or a final answer; it is the ordered source-baseline ledger. Produce `read_user_email` / `gmail_user_search_threads` for unread mail first, then `read_morning_site_list` / `read_morning_briefing_sites` for configured recurring sites, or record the exact Gmail/site-list auth, access, account, configuration, data, query, or material blocker for the missing row. While either baseline row is absent and unblocked, the pass is interrupted: no downstream site check, live market snapshot, source interpretation, action grouping, assembly, handoff, final answer, verification, closure, or remembered-context rationale should continue.

Friction 1092 confirms the same source-baseline stall remains in the evaluated three-enactment window: unread Gmail was absent from all three direct trails, and two trails also lacked the configured morning site-list read before browser/site-check activity. For future `morning_briefing` passes, treat the first selected morning-briefing surface as an interruption unless the ordered baseline ledger is already visible: `read_user_email` / `gmail_user_search_threads` for unread mail first, then `read_morning_site_list` / `read_morning_briefing_sites` for configured recurring sites, or the concrete blocker for the exact missing row. A site-list-only start still requires the unread-Gmail row next; a browser/site-check start still requires unread Gmail and then the configured-site row before any further check, live snapshot, source interpretation, assembly, final answer, verification, handoff, closure, or rationale from remembered context.
