---
id: rule_morning_briefing_unread_first
name: 'Opening gate: unread mail, then configured sites'
---
Friction 997 confirms the `morning_briefing` `reads_morning_sources_before_assembling` stall remains genuine after repeated source-ledger guidance: in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66`, the pass reached `read_morning_briefing_sites` while still omitting `gmail_user_search_threads`, and the two preceding evaluated passes reached browser/site-check behavior without either recurring source row or a concrete source blocker. For future morning briefings, a site-list-only start is not a valid partial opening and cannot be followed by another source check, browser/site interpretation, live market snapshot, action/watch/routine grouping, assembly, handoff, final answer, closure, or rationale from remembered context. The immediate next visible move must be `read_user_email` / `gmail_user_search_threads` for unread mail, or a concrete Gmail auth, access, data, configuration, or material blocker; only after that row is visible or blocked may the configured-site-list row count as the second source. If both rows are missing, recover in order with unread Gmail first and configured sites second, or stop with the exact blocker for the missing row.

Friction 1018 confirms the same `morning_briefing` source-baseline failure remains live in the evaluated three-enactment window: unread Gmail was absent in every pass, and two passes also lacked the configured recurring-site-list read before site-check activity. Treat a missing unread-mail row as the first interruption point even when a configured-site-list row is present. The next visible morning-briefing move must be `read_user_email` reaching `gmail_user_search_threads`, or a concrete Gmail auth, access, account, data, query, configuration, or material blocker; if both source rows are absent, recover in order with unread Gmail first and configured sites second. Do not continue to site checks, live market snapshots, source interpretation, action/watch/routine grouping, assembly, handoff, final answer, closure, or rationale from remembered morning context while either baseline row remains absent and unblocked.

Friction 1020 confirms this remains a practice-quality affordance-coverage failure for the same evaluated three-enactment window: target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` produced the configured site-list row while `gmail_user_search_threads` was still absent, and the two adjacent passes reached browser site-check activity without either recurring source row or a concrete source blocker. For future `morning_briefing` passes, treat the source-baseline ledger as the first work product, not a recoverable detail after site work begins. If unread Gmail is absent and unblocked, the immediate next visible move is `read_user_email` / `gmail_user_search_threads` or the exact Gmail blocker, even when `read_morning_briefing_sites` is already present. If both baseline rows are absent, recover in order with unread Gmail first and configured sites second; do not continue into browser checks, live snapshots, source interpretation, action/watch/routine grouping, assembly, handoff, final answer, closure, or rationale from remembered context while either baseline row remains absent and unblocked.
