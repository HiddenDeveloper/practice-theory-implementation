---
id: rule_morning_briefing_unread_first
name: 'Opening gate: unread mail, then configured sites'
---
Friction 903 confirms the same `morning_briefing` quality-affordance coverage failure remains genuine in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and its three-enactment evaluated window: the newest pass reached the configured site-list material while still omitting `gmail_user_search_threads`, and the two prior passes reached site-check work without either recurring source row. For future morning-briefing passes, unread Gmail is the first non-skippable source ledger row. If `read_morning_site_list` / `read_morning_briefing_sites` appears before `read_user_email` / `gmail_user_search_threads`, the pass is interrupted immediately and the next visible move must be the unread-Gmail read or a concrete Gmail auth/access/data/material blocker. If browser/site-check work appears before both recurring rows are present or blocked, the only valid continuation is the missing source row(s) in order: Gmail first, then configured sites. Do not proceed to source interpretation, live market snapshots, action/watch/routine grouping, assembly, final answers, handoffs, or closure while either recurring source row remains absent and unblocked.

Friction 910 confirms the source-read coverage stall remains live in the same target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and its three-enactment evaluated window: unread email was absent in all three passes, and the configured usual-site list was absent in two. For future `morning_briefing` passes, the first visible source work must be `read_user_email` reaching `gmail_user_search_threads` for unread mail, followed by `read_morning_site_list` reaching `read_morning_briefing_sites` for configured recurring sites, unless the exact missing source row has a concrete Gmail/site-list auth, access, configuration, data, or material blocker. A site-list-only row, browser site check, invalid-URL result, market snapshot, remembered context, final answer, handoff, or assembly attempt is an interrupted briefing while either recurring source row remains unread and unblocked; the immediate next visible move must be the missing row in that order or its blocker.

Friction 913 confirms the same `reads_morning_sources_before_assembling` coverage concern remains genuine in the three-enactment window for target `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66`: no pass showed `gmail_user_search_threads` for unread mail, and two of the three also lacked `read_morning_briefing_sites` for configured recurring sites or a concrete source blocker. Treat the two-row source baseline as the first visible work product of every morning briefing, not as context that can be reconstructed after downstream work. The opening order is unread Gmail first, then the configured usual-site list; if a site-list read, browser/site check, invalid URL handling, live market snapshot, synthesis, assembly, final answer, or handoff appears while either source row is absent and unblocked, the briefing is interrupted and the only valid continuation is the exact missing source read in order or a concrete Gmail/site-list auth, access, configuration, data, or material blocker. Do not complete or summarize a morning briefing from site-list-only, browser-check, remembered, or market-snapshot context while the unread-email row or configured-site-list row remains unread and unblocked.

Friction 920 confirms the same source-coverage concern remains live in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66`: the latest evaluated pass reached `read_morning_briefing_sites` without `gmail_user_search_threads`, while the two prior evaluated passes reached browser site-check work without either recurring source row or a concrete blocker. For future `morning_briefing` passes, a configured-site-list row without an earlier unread-Gmail row is not partial progress; it is an interrupted opening ledger. The immediate next visible move must be `read_user_email` / `gmail_user_search_threads` for unread mail, or a concrete Gmail auth, access, data, configuration, or material blocker that limits the briefing. If neither recurring row is present, recover in order: unread Gmail first, then `read_morning_site_list` / `read_morning_briefing_sites` or its exact blocker. Do not continue to browser/site checks, live market snapshots, source interpretation, assembly, handoff, final answer, or closure while either recurring source row remains absent and unblocked.

Friction 923 confirms the evaluated `morning_briefing` failure is still the opening work product itself: across the three-enactment window, unread email was never read, and two passes also skipped the configured site-list read before site-check work, with no concrete source blocker. For future morning briefings, the first source-bearing row must be `read_user_email` / `gmail_user_search_threads` for unread mail, or a concrete Gmail auth, access, data, configuration, or material blocker; only after that row is visible may the pass move to `read_morning_site_list` / `read_morning_briefing_sites` for configured recurring sites, or its exact blocker. If a pass has already reached site-list-only, browser/site checking, invalid URL handling, live market snapshots, source interpretation, assembly, final answer, handoff, or closure while either source row remains absent and unblocked, the practice is interrupted and the immediate next visible move is the missing source row in order: unread Gmail first, then configured sites. Do not summarize around the missing source ledger or treat downstream site work as a substitute for either recurring source row.

Friction 926 confirms the same `reads_morning_sources_before_assembling` quality concern persists in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and its three-enactment evaluated window: unread Gmail was not read in any pass, and two passes also lacked the configured recurring site-list read. For future `morning_briefing` passes, the opening source ledger is not optional periodic variation and cannot be satisfied by bundle prose, remembered morning context, browser/site checks, market snapshots, source interpretation, assembly, final answers, handoffs, or closure. The first visible source move must be `read_user_email` reaching `gmail_user_search_threads` for unread mail, or a concrete Gmail auth/access/data/configuration/material blocker; only after that row is visible or blocked may the pass proceed to `read_morning_site_list` reaching `read_morning_briefing_sites`, or its exact site-list blocker. If any downstream briefing surface appears first, the pass is interrupted and the only permitted continuation is the missing source row in order, Gmail first and configured sites second, or the exact blocker for that row.

Friction 932 confirms the same `quality_affordance_coverage` failure remains live despite the bundle, understanding, affordance guidance, rules, and invariants already naming the Gmail-first source ledger: in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66`, the pass read the configured site list but still omitted `gmail_user_search_threads`, and the two prior evaluated passes reached site-check work without either recurring source row or a concrete source blocker. For future `morning_briefing` passes, a configured-site-list row before unread Gmail is an interrupted state at that exact moment. The immediate next visible move must be `read_user_email` / `gmail_user_search_threads` for unread mail, or a concrete Gmail auth/access/data/configuration/material blocker that explicitly limits the briefing; do not continue to browser/site checks, live market snapshots, source interpretation, action/watch/routine grouping, assembly, handoff, final answer, or closure from a site-list-only row while unread Gmail remains absent and unblocked.

Friction 936 confirms this remains a genuine `practice_quality_source_coverage` failure across another three-enactment `morning_briefing` window: the latest target again read the configured site list while omitting unread email, and the two prior runs reached browser site checks without either recurring source row or a concrete blocker. For future morning briefings, treat the opening ledger as unfinished until `read_user_email` reaches `gmail_user_search_threads` for unread mail, then `read_morning_site_list` reaches `read_morning_briefing_sites` for configured recurring sites, or the exact missing row has a concrete Gmail/site-list auth, access, configuration, data, or material blocker. A site-list-only start, browser-site check, live market snapshot, source interpretation, action/watch/routine grouping, assembly, final answer, handoff, or closure is not a recoverable summary point; it is an interruption whose immediate next visible move is the missing source row in order, Gmail first and configured sites second, or that row's exact blocker.
