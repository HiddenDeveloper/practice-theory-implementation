---
id: rule_morning_briefing_no_source_read_is_unfinished
name: No source read means the briefing has not started
---
Friction 821 confirms the source-coverage gap remains genuine after another evaluated `morning_briefing` window: all three recent passes still lacked `gmail_user_search_threads`, and two also lacked `read_morning_briefing_sites`, even though the bundle and evaluation already name those rows as the recurring source baseline. Treat the first missing source row as an immediate recovery obligation, not a reminder for final assembly. At the start of every morning briefing, and after any site-list read, browser or one-off site check, invalid URL result, live snapshot, supplied-source interpretation, synthesis, handoff, final answer, or closure attempt, inspect whether the current trail contains `read_user_email` reaching `gmail_user_search_threads` for unread mail or a concrete Gmail auth/access/data/material blocker. If not, the next visible move must be that Gmail unread read or blocker. Only after Gmail is present or blocked may `read_morning_site_list` / `read_morning_briefing_sites` satisfy the second row; if that row is absent and unblocked, it becomes the next visible move. No downstream briefing work counts as progress, and no completed morning orientation should be presented, while either recurring source row is absent and unblocked.

Friction 886 confirms the same practice-quality concern remains live in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and its three-pass evaluated window: unread email was absent in all three passes, and the configured recurring site-list read was absent in two, with no concrete source blocker in the direct step reads. For future `morning_briefing` passes, the first visible work product is the two-row source-baseline ledger itself: `read_user_email` / `gmail_user_search_threads` for unread mail first, then `read_morning_site_list` / `read_morning_briefing_sites` for configured recurring sites, unless the exact row has a concrete Gmail/site-list auth, access, configuration, data, or material blocker. If the pass has already reached browser site checks, invalid-URL handling, live market snapshots, source interpretation, action/watch/routine grouping, assembly, final answer, handoff, or closure before that baseline is complete or blocked, the pass is interrupted; the only permitted continuation is the missing baseline read or its exact blocker, not more downstream briefing work or a summary of the gap.
