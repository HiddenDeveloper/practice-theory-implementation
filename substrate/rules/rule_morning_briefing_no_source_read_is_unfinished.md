---
id: rule_morning_briefing_no_source_read_is_unfinished
name: No source read means the briefing has not started
---
Friction 821 confirms the source-coverage gap remains genuine after another evaluated `morning_briefing` window: all three recent passes still lacked `gmail_user_search_threads`, and two also lacked `read_morning_briefing_sites`, even though the bundle and evaluation already name those rows as the recurring source baseline. Treat the first missing source row as an immediate recovery obligation, not a reminder for final assembly. At the start of every morning briefing, and after any site-list read, browser or one-off site check, invalid URL result, live snapshot, supplied-source interpretation, synthesis, handoff, final answer, or closure attempt, inspect whether the current trail contains `read_user_email` reaching `gmail_user_search_threads` for unread mail or a concrete Gmail auth/access/data/material blocker. If not, the next visible move must be that Gmail unread read or blocker. Only after Gmail is present or blocked may `read_morning_site_list` / `read_morning_briefing_sites` satisfy the second row; if that row is absent and unblocked, it becomes the next visible move. No downstream briefing work counts as progress, and no completed morning orientation should be presented, while either recurring source row is absent and unblocked.

Friction 886 confirms the same practice-quality concern remains live in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and its three-pass evaluated window: unread email was absent in all three passes, and the configured recurring site-list read was absent in two, with no concrete source blocker in the direct step reads. For future `morning_briefing` passes, the first visible work product is the two-row source-baseline ledger itself: `read_user_email` / `gmail_user_search_threads` for unread mail first, then `read_morning_site_list` / `read_morning_briefing_sites` for configured recurring sites, unless the exact row has a concrete Gmail/site-list auth, access, configuration, data, or material blocker. If the pass has already reached browser site checks, invalid-URL handling, live market snapshots, source interpretation, action/watch/routine grouping, assembly, final answer, handoff, or closure before that baseline is complete or blocked, the pass is interrupted; the only permitted continuation is the missing baseline read or its exact blocker, not more downstream briefing work or a summary of the gap.

Friction 889 confirms this same source-coverage failure is still recurring in the evaluated `morning_briefing` window: the target pass read only the configured site list while omitting unread Gmail, and the two adjacent passes lacked both required source rows while moving into browser/site-check gaps. For future morning-briefing passes, a configured-site-list row without a prior unread-Gmail row is not a partial success; it is an interrupted briefing whose immediate next visible move must be `read_user_email` reaching `gmail_user_search_threads`, or a concrete Gmail auth/access/data/material blocker that explicitly limits the briefing. If both recurring rows are absent, the recovery order is unread Gmail first, then the configured site-list read or its concrete blocker. Browser checks, invalid URL handling, live snapshots, source interpretation, action/watch/routine grouping, final answers, handoffs, and closure must wait while either recurring source remains absent and unblocked.

Friction 1015 confirms this is still a source-coverage practice-quality concern after the same target path and its evaluated window: one pass read only `read_morning_briefing_sites`, and two adjacent passes reached browser/site-check activity while both recurring source rows were absent. For future `morning_briefing` passes, treat any source-adjacent progress without the ordered baseline as unfinished, not partial grounding. If unread Gmail is absent and unblocked, the next visible move must be `read_user_email` / `gmail_user_search_threads` or a concrete Gmail blocker, even when the configured site list has already been read. Only after unread Gmail is present or blocked may the configured recurring-site-list row be satisfied; browser checks, invalid-URL handling, live snapshots, source interpretation, assembly, final answer, handoff, closure, or rationale from remembered morning context must not continue while either baseline row remains absent and unblocked.

Friction 1024 confirms the same opening source-baseline stall remains a live `morning_briefing` practice-quality concern: across the evaluated three-enactment window, unread Gmail was absent every time, and two passes also lacked the configured recurring-site-list read or a concrete blocker before downstream site-check activity. For future `morning_briefing` passes, treat the missing source-baseline row as the first visible interruption, not as acceptable periodic variation. Before any downstream site check, browser check, live market snapshot, source interpretation, action/watch/routine grouping, assembly, handoff, final answer, closure, or rationale from remembered context, the current enactment must show `read_user_email` / `gmail_user_search_threads` for unread mail first and then `read_morning_site_list` / `read_morning_briefing_sites` for configured recurring sites, or must record the exact Gmail or site-list auth, access, account, configuration, data, query, or material blocker. If site-list or site-check activity has already appeared while unread Gmail is absent and unblocked, the only valid continuation is the unread Gmail row or its blocker; if both recurring rows are absent and unblocked, recover in order with unread Gmail first and configured sites second.
