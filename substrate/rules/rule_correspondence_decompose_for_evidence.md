---
id: rule_correspondence_decompose_for_evidence
name: Decompose for evidence
---
Friction 1058 confirms the `grounds_correspondent_before_offering` quality concern remains across the evaluated correspondent window: three of five passes still lacked `gmail_user_get_thread`, including one that found a user thread id but stopped at snippets and two that surfaced Gmail authorization errors before retrieval. For future correspondent passes, `gmail_user_search_threads` is only a locator row. When correspondence work may offer a draft, friction, stance invitation, readiness, limit, or other response grounded in the message, the next visible grounding move after a live user-thread search that yields a usable thread id must be `read_user_email` / `gmail_user_get_thread` for the selected live thread; if retrieval is blocked by authorization, material availability, missing id, or data access, record that concrete blocker before any offering and confine the response to supplied text, visible search metadata, or the named access limit. Do not treat test-mailbox search, snippets, authorization errors from adjacent attempts, remembered correspondent context, or bundle prose as a substitute for the live thread retrieval row.

Friction 1072 confirms the same grounding gap remains live in the evaluated `correspondent` window: the latest missing pass returned live Gmail thread id `19e96aa1f9d1a621` from `gmail_user_search_threads` and then moved to test-mailbox search without `gmail_user_get_thread`, while two other missing passes ended at Gmail authorization errors. For future correspondent passes, a live search result that returns one or more thread ids interrupts the offering path until the selected live thread is retrieved with `read_user_email` / `gmail_user_get_thread`, or until a concrete auth/access/material/missing-selection blocker is recorded. Do not offer relationship context, readiness, stance framing, friction, limit language, or draft text as grounded in the thread from search snippets, test-mailbox search, failed auth attempts, remembered correspondence, or bundle prose; if retrieval is blocked, the only valid correspondence offer is explicitly confined to supplied text, visible search metadata, or the named access limit.

Friction 1085 narrows the continuing failure to the post-search snippet stop: when `gmail_user_search_threads` returns a usable live thread id, snippets and search metadata are not correspondence grounding. The immediate next correspondence-grounding move must be `read_user_email` / `gmail_user_get_thread` for the selected live thread before any draft, friction, stance invitation, readiness judgement, limit, relationship context, or other offering that depends on the message body. Authorization, configuration, access, missing-selection, or material failures are blockers to name explicitly; they do not count as discretionary retrieval omissions, and they only permit an offer that is visibly confined to supplied text, visible search metadata, or the named access limit.

Friction 1098 confirms the same quality gap persists after the rule already names the post-search retrieval gate: across the evaluated `correspondent` window, three of five passes still lacked `gmail_user_get_thread`, including target enactment `408e9d39-f733-4d66-b00e-5100f1ad3036`, which found live Gmail thread id `19e96aa1f9d1a621` and then moved to test-mailbox search without retrieving the live thread. For future correspondent passes, once `gmail_user_search_threads` returns a usable live thread id, the pass is interrupted: the immediate next correspondence-grounding move must be `read_user_email` / `gmail_user_get_thread` for the selected live thread, or a concrete auth/configuration/access/material/missing-selection blocker. Test-mailbox search, snippets, adjacent authorization errors, remembered correspondent context, bundle prose, or a later testing path must not stand in for the live thread retrieval row; while that row remains absent and unblocked, do not proceed to draft, readiness judgement, friction, stance invitation, limit language, relationship context, verification, or final correspondence offering except one visibly confined to supplied text, visible search metadata, or the named access limit.
