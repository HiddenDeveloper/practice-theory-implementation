---
id: rule_correspondence_decompose_for_evidence
name: Decompose for evidence
---
Friction 1039 confirms the same `grounds_correspondent_before_offering` quality concern on target enactment `408e9d39-f733-4d66-b00e-5100f1ad3036`: a reachable live Gmail thread id (`19e96aa1f9d1a621`) was found, but the pass stopped at live and test search snippets instead of retrieving the selected live thread with `read_user_email` / `gmail_user_get_thread`. For future correspondent passes, once a live Gmail search returns a thread id that could ground the correspondence, the next grounding move must retrieve that live thread or record the exact retrieval blocker. A test-mailbox search result, snippet-only search metadata, or authorization-error context from neighboring cases must not be treated as a substitute for live-thread retrieval; any response before retrieval must be explicitly limited to supplied text, visible search metadata, or the named access limit.

Friction 1047 confirms the broader `correspondent` quality concern remains across a five-enactment window: three passes reached only Gmail search results or Gmail search errors and never retrieved the live thread with `gmail_user_get_thread`, leaving later correspondence readiness ungrounded. For future correspondent passes, a Gmail search row is only a locator, not a correspondence basis. Before offering a reply, drafting, stating readiness, inviting or taking a correspondence stance, declaring a correspondence limit, or otherwise proceeding as though the message has been read, the same enactment must either retrieve the selected live thread with `read_user_email` / `gmail_user_get_thread`, or record a concrete Gmail retrieval/auth/material blocker and explicitly confine the response to user-supplied text, visible search metadata, or that named access limit. Do not treat test-mailbox search, search snippets, remembered context, neighboring authorization errors, or bundle prose as a substitute for the live thread row.
