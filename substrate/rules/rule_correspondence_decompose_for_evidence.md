---
id: rule_correspondence_decompose_for_evidence
name: Decompose for evidence
---
Friction 784 confirms the remaining failure is a successful-search stopping state: a `correspondent` pass can return concrete live Gmail thread ids from `gmail_user_search_threads` and still stop without `gmail_user_get_thread`. Treat that as an unfinished correspondence pass, not as a narrow completion. Once live Gmail search returns any candidate thread id, subject, snippet, or enough metadata to choose the relevant thread, the immediate next visible state must be `read_user_email` reaching `gmail_user_get_thread` for that user thread, or a `declare_correspondence_limit` / narrowly scoped `correspondence_offer` that names the exact reason retrieval cannot proceed and confines the work to supplied text or visible search metadata. Do not close, final-answer, invite stance, draft, analyze, or substitute `gmail_test_search_threads` while a selected live thread id remains unretrieved and no retrieval blocker has been named.

Friction 791 confirms this remains a practice-quality grounding stall across a recent `correspondent` window, including search-only paths and credential-error paths. Treat `gmail_user_search_threads` as only the discovery row, not the correspondence ground. A `correspondent` pass that has searched Gmail but has not reached `gmail_user_get_thread` is interrupted: the next visible move must be `read_user_email` reaching `gmail_user_get_thread` for the selected live thread, or `declare_correspondence_limit` / a narrowly scoped `correspondence_offer` naming the concrete auth/access/data/material blocker and limiting any offering to supplied text or visible search metadata. If credentials or OAuth are missing, name that retrieval blocker explicitly and do not draft, analyze, invite stance, or final-answer as though the thread body had been read.

Friction 795 confirms the correspondence-grounding gap remains genuine across the evaluated window: three of five recent `correspondent` enactments lacked `gmail_user_get_thread`, including one pass where live and test searches returned threads and two passes where Gmail authorization failures stopped retrieval. For future `correspondent` passes, a reachable live Gmail thread is not grounded by search metadata alone. After `gmail_user_search_threads` returns any candidate live thread, the immediate next move must be `read_user_email` reaching `gmail_user_get_thread` for the selected user thread, unless the practitioner first records a concrete retrieval blocker through `declare_correspondence_limit` or a narrowly scoped correspondence offering. Authorization, refresh-token, OAuth-client, data, or material failures count only when named as blockers, and they must limit the pass to supplied text or visible search metadata; they do not authorize drafting, analysis, stance invitation, final answer, or substitution with `gmail_test_search_threads` as if the thread body had been read.

Friction 809 confirms the same affordance-coverage concern persists across the evaluated `correspondent` window: three of five recent passes reached `gmail_user_search_threads` without the grounding `gmail_user_get_thread` row, while nearby contrast passes showed the get-thread path is reachable. Treat a search-only live Gmail pass as ungrounded correspondence, not as enough evidence to offer a reply, invite stance, draft, analyze, or close. After search returns a candidate thread or metadata, the only permitted next states are `read_user_email` reaching `gmail_user_get_thread` for the selected thread, or `declare_correspondence_limit` / a narrowly scoped `correspondence_offer` that names the concrete retrieval blocker and confines any statement to supplied text or visible search metadata.

Friction 813 confirms the coverage failure remains genuine after another evaluated window: three of five recent `correspondent` enactments reached `gmail_user_search_threads` without a later `gmail_user_get_thread`, while contrast enactments showed the get-thread path is reachable. For future `correspondent` passes, treat live Gmail search as an ungrounded discovery row until the selected thread body is read. If a search returns candidate live thread ids, subjects, snippets, or other metadata sufficient to identify the relevant correspondence, the immediate next visible move must be `read_user_email` reaching `gmail_user_get_thread` for that selected thread, or `declare_correspondence_limit` / a narrowly scoped `correspondence_offer` naming the concrete retrieval blocker and explicitly limiting the pass to supplied text or visible search metadata. Do not draft, analyze, invite stance, final-answer, close, or substitute test-search output while the selected live thread remains unretrieved and no blocker has been recorded.

Friction 826 confirms this remains a practice-quality affordance-coverage concern across the evaluated `correspondent` window, not a one-off step lapse: three of five recent correspondent enactments lacked `gmail_user_get_thread` after search or access failure, while comparison enactments showed thread retrieval is reachable. For future `correspondent` passes, search or authorization failure must not become the resting state for correspondence work. After any `gmail_user_search_threads` attempt, the next visible grounding move must be `read_user_email` reaching `gmail_user_get_thread` for the selected live thread when one is available, or `declare_correspondence_limit` / a narrowly scoped `correspondence_offer` that names the concrete retrieval blocker (including refresh-token, OAuth-client, auth, data, or material failure) and confines any offering to supplied text or visible search metadata. Do not draft, analyze, invite stance, final-answer, close, or substitute test-search output while the live thread body remains unread and no blocker has been recorded.
