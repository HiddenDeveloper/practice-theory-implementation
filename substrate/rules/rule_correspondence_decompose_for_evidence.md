---
id: rule_correspondence_decompose_for_evidence
name: Retrieve the live thread before correspondence offerings
---
Friction 1131 confirms the same `grounds_correspondent_before_offering` concern remains a practice-quality affordance-coverage failure across the evaluated five-enactment Correspondent window: three enactments reached live Gmail thread search without `gmail_user_get_thread`, including target enactment `408e9d39-f733-4d66-b00e-5100f1ad3036`, which returned thread id `19e96aa1f9d1a621` and then detoured into `gmail_test_search_threads`. For future Correspondent passes, a successful `gmail_user_search_threads` row that yields any usable live thread id creates an immediate interrupt: the next visible Correspondent grounding row must be `read_user_email` / `gmail_user_get_thread` for the selected live thread, or a concrete blocker naming auth, configuration, access, material availability, or missing selection. Do not continue into test-mailbox search, setup checks, remembered context, snippets, readiness or stance work, draft/friction/limit language, verification, or final correspondence offering while the selected live thread remains unretrieved and unblocked; any response before retrieval must be visibly confined to supplied text, visible search metadata, or the named access blocker.

Friction 1155 confirms the quality concern remains a retrieval/grounding gap rather than an unreviewed-send concern: across the five-enactment Correspondent window, three passes did not reach `gmail_user_get_thread` after live thread search or search failure, so the practice often could not show grounding in reachable user-thread content. For future Correspondent passes, `gmail_user_search_threads` is not enough when it returns a candidate thread id, and a failed search is not enough to proceed as though thread content has been inspected. The next visible move after a candidate live thread is found must be `read_user_email` / `gmail_user_get_thread`; if auth, configuration, access, missing selection, or material availability prevents that retrieval, record that concrete blocker and limit any correspondence offering to the supplied message text, visible search metadata, or the blocker itself. Do not detour into `gmail_test_search_threads`, recalled correspondent context, draft work, readiness language, or final reply guidance while a reachable live thread remains unfetched and unblocked.
