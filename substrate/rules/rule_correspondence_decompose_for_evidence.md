---
id: rule_correspondence_decompose_for_evidence
name: Decompose for evidence
---
Friction 963 confirms the same `grounds_correspondent_before_offering` quality gap remains in target enactment `408e9d39-f733-4d66-b00e-5100f1ad3036`: three of five evaluated correspondent passes lacked `gmail_user_get_thread`. A successful `gmail_user_search_threads` result that returns thread `19e96aa1f9d1a621` is not grounded until that exact live thread is retrieved, and credential/OAuth/client/refresh-token errors do not count as disciplined variation unless the same enactment records them as concrete retrieval blockers. After any live Gmail search or access attempt for correspondence, the immediate next correspondence-grounding move must be `read_user_email` / `gmail_user_get_thread` for the selected thread, or a blocker naming the exact credential, OAuth, account, data, selection, or material condition that prevents retrieval; do not continue to test-search substitution, drafting, analysis, stance invitation, final answer, closure, or any completion-shaped offer while the live thread remains unread and unblocked, except for language explicitly confined to supplied text or visible search metadata.

Friction 976 confirms this remains a live `correspondent` practice-quality concern after the rule already named the retrieval gate: in the evaluated window, one successful live user-thread search stopped without `gmail_user_get_thread`, and two access-error traces could not ground retrieval at all, while passing traces showed `gmail_user_search_threads` followed by `gmail_user_get_thread`. For future correspondent passes, treat a live Gmail search result, a test-search substitution after live search, or a Gmail credential/OAuth/client/refresh-token/access error as an interrupted grounding state until the same enactment either retrieves the selected live thread with `read_user_email` / `gmail_user_get_thread` or records a concrete retrieval blocker naming the exact failed condition. Do not move from search-only, test-search-only, or access-error context into drafting, correspondence analysis, stance invitation, final answer, closure, or any completion-shaped offer unless the response is explicitly confined to supplied text or visible search/error metadata and names the retrieval limit.

Friction 1014 confirms the same grounding gap at the practice-quality layer: in three of five recent `correspondent` enactments, the pass reached live Gmail search or access-error context but did not retrieve the full live thread before stopping, while a nearby contrast pass showed `gmail_user_search_threads` followed by `gmail_user_get_thread`. For future correspondent passes, a successful search snippet, a test-mailbox substitute after live search, or a Gmail auth/client/refresh-token/setup error is not enough to offer correspondence artifacts as grounded thread work. The pass must either retrieve the selected live thread with `read_user_email` / `gmail_user_get_thread`, or record the exact retrieval blocker and confine any response to supplied text, visible search metadata, or the named access limit.

Friction 1023 confirms the correspondent grounding concern remains live across the evaluated window: three of five correspondent enactments lacked `gmail_user_get_thread`, including target `408e9d39-f733-4d66-b00e-5100f1ad3036`, which stopped after live and test search results without retrieving the live thread, and access-error cases that stopped without a concrete retrieval blocker. Treat search/snippet evidence, test-search substitution, and Gmail authorization or credential errors as insufficient for person, thread, and relationship grounding. Before any correspondence work that offers analysis, a draft, a stance invitation, a final answer, or closure from Gmail context, the same enactment must either retrieve the selected live thread with `read_user_email` / `gmail_user_get_thread`, or record the exact retrieval blocker and explicitly confine the response to supplied text, visible search metadata, or the named access limit.
