---
id: rule_correspondence_decompose_for_evidence
name: Retrieve the live thread before correspondence offerings
---
Friction 1131 confirms the same `grounds_correspondent_before_offering` concern remains a practice-quality affordance-coverage failure across the evaluated five-enactment Correspondent window: three enactments reached live Gmail thread search without `gmail_user_get_thread`, including target enactment `408e9d39-f733-4d66-b00e-5100f1ad3036`, which returned thread id `19e96aa1f9d1a621` and then detoured into `gmail_test_search_threads`. For future Correspondent passes, a successful `gmail_user_search_threads` row that yields any usable live thread id creates an immediate interrupt: the next visible Correspondent grounding row must be `read_user_email` / `gmail_user_get_thread` for the selected live thread, or a concrete blocker naming auth, configuration, access, material availability, or missing selection. Do not continue into test-mailbox search, setup checks, remembered context, snippets, readiness or stance work, draft/friction/limit language, verification, or final correspondence offering while the selected live thread remains unretrieved and unblocked; any response before retrieval must be visibly confined to supplied text, visible search metadata, or the named access blocker.

Friction 1155 confirms the quality concern remains a retrieval/grounding gap rather than an unreviewed-send concern: across the five-enactment Correspondent window, three passes did not reach `gmail_user_get_thread` after live thread search or search failure, so the practice often could not show grounding in reachable user-thread content. For future Correspondent passes, `gmail_user_search_threads` is not enough when it returns a candidate thread id, and a failed search is not enough to proceed as though thread content has been inspected. The next visible move after a candidate live thread is found must be `read_user_email` / `gmail_user_get_thread`; if auth, configuration, access, missing selection, or material availability prevents that retrieval, record that concrete blocker and limit any correspondence offering to the supplied message text, visible search metadata, or the blocker itself. Do not detour into `gmail_test_search_threads`, recalled correspondent context, draft work, readiness language, or final reply guidance while a reachable live thread remains unfetched and unblocked.

Friction 1169 confirms the same `grounds_correspondent_before_offering` quality gap remains live after additional evaluation: three of five recent Correspondent enactments did not reach `gmail_user_get_thread` after live thread search or attempted search, while passing comparison enactments showed the get-thread path is reachable. For future Correspondent passes, any live thread-search row starts the correspondence-grounding gate. If it returns a usable thread id, the immediate next correspondence-grounding move must be `read_user_email` / `gmail_user_get_thread` for the selected thread; if the search itself fails because of OAuth, configuration, access, or material availability, record that concrete blocker and keep any response explicitly limited to supplied text, visible search metadata, or the blocker. Do not continue into test-mailbox search, recalled context, stance/readiness work, draft offering, friction surfacing, verification, or final reply guidance from snippets, remembered context, or credential errors while reachable thread contents remain unread and unblocked.

Friction 1177 confirms the failure remains live in the evaluated five-enactment Correspondent window: three recent enactments lacked `gmail_user_get_thread`, including target `408e9d39-f733-4d66-b00e-5100f1ad3036`, where live Gmail search returned thread ids/snippets and no thread body was retrieved. Treat returned live thread ids as evidence handles, not correspondence context. When `gmail_user_search_threads` returns any usable thread id, the next visible correspondence-grounding move must be `read_user_email` / `gmail_user_get_thread` for the selected live thread, or a concrete blocker naming auth, configuration, access, material availability, or missing selection; do not substitute `gmail_test_search_threads`, snippets, remembered context, readiness language, draft work, or final correspondence guidance while the reachable live thread remains unfetched and unblocked.

Friction 1183 confirms the same `grounds_correspondent_before_offering` quality signal remains a genuine Correspondent practice concern: in the evaluated five-enactment window, three passes reached live Gmail search but did not retrieve the thread before correspondence-facing moves, and target enactment `408e9d39-f733-4d66-b00e-5100f1ad3036` again showed `gmail_user_search_threads` returning thread `19e96aa1f9d1a621` with no `gmail_user_get_thread`. For future Correspondent passes, a live Gmail search result or search failure is not a sufficient person/thread context. If `gmail_user_search_threads` returns a usable thread id, the immediate next visible grounding move must be `read_user_email` / `gmail_user_get_thread` for the selected live thread; if retrieval cannot happen because of OAuth, configuration, access, material availability, or missing selection, record that concrete blocker. Do not proceed into test-mailbox search, draft or reply offerings, stance/readiness language, recalled context, verification, or final correspondence guidance from search snippets, access errors, or remembered context while the live thread remains unretrieved and unblocked; any limited response before retrieval must explicitly stay within supplied text, visible search metadata, or the named blocker.

Friction 1190 confirms the same `grounds_correspondent_before_offering` gap remains real across another quality window: three of five evaluated Correspondent enactments lacked `gmail_user_get_thread`, including target `408e9d39-f733-4d66-b00e-5100f1ad3036`, where live search returned thread evidence but no retrieval followed, and two runs that stopped at Gmail auth errors. For future Correspondent passes, search results and auth errors are both unfinished grounding states. If `gmail_user_search_threads` returns any usable thread id, the immediate next visible move must be `read_user_email` / `gmail_user_get_thread` for the selected live thread; if OAuth, refresh-token, configuration, access, data, material availability, or ambiguous selection prevents retrieval, record that exact blocker through the correspondence surface and confine any output to supplied text, visible search metadata, or the named access blocker. Do not close, verify, detour into test-mailbox search, recall context, draft/reply/stance work, or final correspondence guidance while the selected live thread remains unretrieved and no concrete blocker has been recorded.
