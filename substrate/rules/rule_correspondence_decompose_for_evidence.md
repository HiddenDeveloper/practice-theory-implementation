---
id: rule_correspondence_decompose_for_evidence
name: Decompose for evidence
---
Friction 846 confirms the same correspondence grounding concern remains genuine in target enactment `408e9d39-f733-4d66-b00e-5100f1ad3036` and its five-enactment evaluated window: three passes used `gmail_user_search_threads` without a later `gmail_user_get_thread`, while contrast passes showed thread retrieval was reachable before deeper correspondence or draft work. For future `correspondent` passes, `gmail_user_search_threads` is only a discovery row. When it returns or attempts to locate a live thread, the correspondence work is unfinished until the same enactment either reaches `read_user_email` / `gmail_user_get_thread` for the selected live thread, or records a concrete retrieval blocker through `declare_correspondence_limit` or a narrowly scoped `correspondence_offer` that confines any statement to supplied text or visible search metadata. Do not treat `gmail_test_search_threads`, snippets, subjects, credential errors, OAuth setup errors, or search metadata as a substitute for the unread thread body, and do not proceed to draft, analyze, invite stance, final-answer, close, or otherwise complete the pass while the selected live thread remains unretrieved and unblocked.

Friction 896 confirms this is still a practice-quality grounding gap after another evaluated window: three of five correspondent enactments searched for Gmail threads but never retrieved the live thread body. Therefore, after `gmail_user_search_threads` is used for a live correspondence task, the immediate next evidence-bearing correspondent move must be `read_user_email` / `gmail_user_get_thread` for the selected thread, or a concrete retrieval blocker naming the credential, OAuth, data, selection, or material condition that prevents retrieval and confining any subsequent offer to supplied text or visible search metadata. Do not continue from a search-only row into drafting, analysis, stance invitation, final answer, closure, or test-search substitution while the live thread body remains unread and unblocked.

Friction 902 confirms the remaining failure is at affordance coverage, not evaluation wording: in three of five recent `correspondent` enactments, the practice reached `gmail_user_search_threads` but did not produce the corresponding `gmail_user_get_thread` row, including a case where search returned concrete thread id `19e96aa1f9d1a621`; two contrast enactments show the search-plus-get path is reachable. Treat `gmail_user_search_threads` as an unfinished locator row. The next correspondence-grounding move after a live thread search must be `read_user_email` / `gmail_user_get_thread` for the selected thread id, unless the same enactment first records a concrete retrieval blocker that names the failed credential, OAuth, missing-id, selection, account, or material condition and limits any subsequent offering to supplied text or visible search metadata only. Do not move from search into `gmail_test_search_threads`, draft/offering work, stance invitation, correspondence limits, final answer, or closure as though the thread were grounded while that retrieval row or blocker is absent.

Friction 909 confirms the same practice-quality concern remains genuine in the inspected `correspondent` window, with one successful `gmail_user_search_threads` result returning concrete thread id `19e96aa1f9d1a621` and stopping before `gmail_user_get_thread`, plus environmental access-error cases where retrieval was not reachable. For future `correspondent` passes, a successful live Gmail search that returns a thread id is not grounded until the same enactment retrieves that exact thread through `read_user_email` / `gmail_user_get_thread`; the immediate next correspondence-grounding row must be that retrieval or a concrete retrieval blocker. Credential, refresh-token, OAuth-client, data, selection, account, or material errors may explain why retrieval cannot happen, but they must be recorded as blockers that confine any subsequent correspondence offer to supplied text or visible search metadata only. Do not treat a successful search result, failed test search, access-error search, subject/snippet metadata, or the bundle's remembered grounding rule as enough to draft, analyze, invite stance, final-answer, close, or otherwise complete correspondence work while the selected live thread body remains unread and unblocked.

Friction 919 confirms the remaining correspondence-grounding failure is the first move after a successful live thread search: in the evaluated window, `gmail_user_search_threads` returned concrete thread id `19e96aa1f9d1a621` in target enactment `408e9d39-f733-4d66-b00e-5100f1ad3036`, but no `gmail_user_get_thread` followed. For future `correspondent` passes, once a live Gmail search returns any thread id, the practice is interrupted until the immediate next correspondence-grounding row is `read_user_email` / `gmail_user_get_thread` for the selected live thread id, or a concrete retrieval blocker naming why that exact id cannot be retrieved. Do not continue from a successful search into `gmail_test_search_threads`, draft/offering work, analysis, stance invitation, correspondence limits, final answer, closure, or any other completion-shaped move while the live thread id remains unretrieved and unblocked; if retrieval cannot be performed, any subsequent statement must be explicitly confined to supplied text or visible search metadata.

Friction 935 confirms the same `grounds_correspondent_before_offering` quality concern still holds across the evaluated `correspondent` window: three of five passes reached live-thread search or access attempts without a same-enactment `gmail_user_get_thread`, while the contrast passes show retrieval is reachable. For future `correspondent` work, search/access attempts are not a completed evidence layer. After `gmail_user_search_threads` locates or attempts to locate a live correspondence thread, the next visible correspondent move must be `read_user_email` / `gmail_user_get_thread` for the selected thread or a concrete retrieval blocker naming the credential, OAuth, data, selection, account, or material condition that prevents retrieval. Until that row exists, do not proceed to drafting, correspondence offers, stance invitation, analysis, final answer, or closure except for a response explicitly confined to supplied text or visible search metadata.

Friction 947 confirms the unresolved `grounds_correspondent_before_offering` coverage gap remains genuine in target enactment `408e9d39-f733-4d66-b00e-5100f1ad3036` and its evaluated five-enactment window: three passes still reached live thread search without `gmail_user_get_thread`, while two contrast passes showed retrieval was reachable. For future `correspondent` passes, a live Gmail search that returns or attempts to locate candidate thread ids is an unfinished locator row. The immediate next correspondence-grounding move must be `read_user_email` / `gmail_user_get_thread` for the selected live thread, or a concrete retrieval blocker naming the credential, OAuth, missing-id, selection, account, data, or material condition that prevents retrieval. Do not continue from search-only evidence into test-search substitution, drafting, correspondence offers, stance invitation, analysis, final answer, closure, or any completion-shaped move while the live thread body remains unread and unblocked; any permitted limited offer must be explicitly confined to supplied text or visible search metadata.
