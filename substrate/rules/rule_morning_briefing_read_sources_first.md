---
id: rule_morning_briefing_read_sources_first
name: Read morning sources before assembling
---
Friction 1095 confirms the `morning_briefing` quality signal `reads_morning_sources_before_assembling` is a genuine affordance-coverage concern: recent passes repeatedly assembled from only one source row or from browser/site-check substitutes while omitting unread Gmail and the configured recurring site-list read, with no concrete blocker. For future morning briefing passes, the first briefing-grounding work products must be both recurring source reads in the same enactment: `read_user_email` / `gmail_user_search_threads` for unread Gmail and `read_morning_site_list` / `read_morning_briefing_sites` for the configured usual sites. A browser/site check, remembered daily context, a prior briefing, bundle prose, or one successful source read is not a substitute for the other required row. If either source read is unavailable, record the concrete auth/access/configuration/data/material blocker before assembling, and confine the briefing to the sources actually read plus the named gap. Do not invoke `assemble_morning_briefing`, produce a final briefing, or close as complete while either required source row remains unread and unblocked.
