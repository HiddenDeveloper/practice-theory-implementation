---
id: rule_morning_briefing_read_sources_first
name: Read morning sources before assembling
---
Friction 1114 confirms the `morning_briefing` source-baseline gate is still failing as a practice-quality affordance-coverage concern: in the evaluated three-enactment window, `gmail_user_search_threads` was absent in all three passes and `read_morning_briefing_sites` was absent in two, with no concrete blockers recorded for the missing rows. For future `morning_briefing` passes, treat the recurring-source baseline as an interruption at the opening of briefing work: the first required visible row is `read_user_email` / `gmail_user_search_threads` for unread Gmail, followed by `read_morning_site_list` / `read_morning_briefing_sites` for the configured usual-site list, unless the immediate next row records a concrete auth, access, configuration, data, or material blocker for the missing source. Do not proceed to site checks, live market reads, assembly, summaries, final briefing output, verification, or closure from bundle prose, remembered morning context, prior source reads, partial site checks, or a single source row while either required baseline row remains absent and unblocked.

Friction 1121 confirms the same `quality_affordance_coverage` stall remains live in target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and its evaluated three-pass window: unread Gmail was absent in all three passes, and two passes also skipped the configured site-list read before site-check activity. For future `morning_briefing` passes, the first selected briefing surface is the interruption point unless the ordered source-baseline ledger is already visible or concretely blocked. The immediate next visible move must be `read_user_email` / `gmail_user_search_threads` for unread mail; only after that row or its exact Gmail auth/access/account/configuration/data/query/material blocker is visible may the pass proceed to `read_morning_site_list` / `read_morning_briefing_sites` for configured recurring sites, or its exact blocker. Do not continue to browser/site checks, live market snapshots, source interpretation, action or routine grouping, assembly, handoff, verification, final briefing output, or closure from remembered morning context, bundle prose, one successful source row, or partial site activity while either baseline row remains absent and unblocked.
