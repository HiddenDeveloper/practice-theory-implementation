---
id: rule_morning_briefing_read_sources_first
name: Read morning sources before assembling
---
Friction 1124 confirms the same source-baseline coverage failure remains genuine in the evaluated three-enactment window for target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66`: the latest pass reached only the configured site-list read, the prior pass went straight to a Guardian browser site check, and the third recorded an invalid-URL site check; none showed `gmail_user_search_threads` for unread mail. For future `morning_briefing` passes, the first selected briefing affordance is the interruption point until unread Gmail is visible or concretely blocked. If `read_morning_site_list` / `read_morning_briefing_sites`, `capture_morning_site_check` / `morning_briefing_browser_site_check`, live market reads, source interpretation, assembly, handoff, final answer, verification, or closure appears before `read_user_email` / `gmail_user_search_threads`, the immediate next visible move must be unread Gmail through that exact row or a concrete Gmail auth/access/account/configuration/data/query/material blocker. A configured site-list row, a direct browser check, an invalid URL result, remembered morning context, bundle prose, or one successful non-email source must not carry the pass forward while unread Gmail remains absent and unblocked.

Friction 1128 confirms the remaining `morning_briefing` quality failure is the missing recurring-source baseline itself, not a missing evaluation: in the evaluated three-enactment window, unread Gmail was absent from every pass and the configured usual-site-list row was absent from two passes even while site-check or briefing work continued. Treat the two-row source ledger as the first work product of the practice. If a pass starts with only the site-list row, a browser/site check, invalid-URL handling, market/source interpretation, handoff, final answer, or any downstream briefing surface, it is interrupted until the exact missing `read_user_email` / `gmail_user_search_threads` row is visible or a concrete Gmail blocker is recorded; after Gmail is visible or blocked, the same rule applies to `read_morning_site_list` / `read_morning_briefing_sites`. Do not count partial source activity, remembered morning context, or bundle prose as source-baseline progress while either required row remains absent and unblocked.

Friction 1160 confirms the `reads_morning_sources_before_assembling` quality signal is still a live source-coverage failure for `morning_briefing`: across the evaluated three-enactment window, every pass missed `read_user_email` / `gmail_user_search_threads`, and two passes also moved into briefing or site-check work without `read_morning_site_list` / `read_morning_briefing_sites`. For future passes, treat the first non-source briefing move as an immediate stop condition unless both source rows are already visible or the exact missing source has a concrete blocker. A configured-site-list-only row is not progress past the gate; after it, the next visible move must still be unread Gmail or its blocker. A browser/site-check row with neither source visible is not a substitute for the recurring site list; the next visible move must be unread Gmail first, then the configured site list, or concrete blockers for those exact rows. Do not assemble, summarize, verify, hand off, close, or continue from site-check context, partial source context, remembered morning context, or bundle prose while either source row remains absent and unblocked.

Friction 1163 confirms the same failure remains a practice-quality affordance-coverage drift in the first visible work product: the evaluated window again missed unread Gmail in all three passes, and two passes proceeded directly to site-check work without the configured site-list read. For future `morning_briefing` passes, choosing any briefing affordance starts at the source ledger, not at the requested downstream task. The next visible move must be `read_user_email` / `gmail_user_search_threads` for unread mail, or a concrete Gmail auth, access, account, configuration, data, query, or material blocker. Only after that row or blocker is visible may the pass proceed to `read_morning_site_list` / `read_morning_briefing_sites`, or its concrete site-list blocker. A site-list-only row, browser/site-check row, invalid-URL result, live market snapshot, source interpretation, prior-day memory, bundle description, or final-answer pressure is an interrupted state, and no briefing synthesis, handoff, verification, closure, or further site work should continue while either recurring source row remains unread and unblocked.
