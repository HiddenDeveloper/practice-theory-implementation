---
id: about_shared_context
name: About the shared context
materials:
- consult_canonical_context
---
Consult CanonicalContext — the current objectives, projects, and open threads shared by the user and AIlumina. Note the relationship to about_the_user: that aggregated read already returns this same CanonicalContext node together with the user profile and self-model in a single read, so if you have already called about_the_user this session, this node is already in hand and re-fetching it here reproduces data you hold and adds nothing. Reach for this single-node read only when you deliberately want CanonicalContext re-read on its own for freshness; to learn what is new since orientation, prefer the recall affordances over re-pulling this static landing node.
