---
id: read_pool
name: Read a pool
materials:
- pm_read_pool
---
Inspect what is already in a pool before authoring or amending.

For Practice Management substrate stewardship, this is the entry affordance, not a supporting read. When the work may reload substrate, check documentation impact, create, amend, author or amend evaluations/invariants, or wire a bundle, invoke this affordance first with the exact pool whose ids or current content the pass will rely on (`teleo_affective`, `understanding`, `rules`, `affordances`, or `materials`). If reload or documentation impact has already happened before the relevant pool read, the immediate next Practice Management move is this affordance for that pool; if any write or bundle wiring step has already occurred without it, stop further substrate action and record the missing-pool-read blocker rather than continuing from persistence, bundle prose, prior trail memory, documentation context, or invariant coverage. This amendment responds to Friction 770's confirmed `uses_substrate_authoring_surface` quality concern: the evaluated window still missed `pm_read_pool` in five of eight Practice Management substrate-stewardship enactments while nearby counterexamples showed this affordance was reachable.

Friction 1161 confirms the miss remains a genuine Practice Management practice-quality concern rather than acceptable periodic variation: five of eight evaluated enactments lacked `pm_read_pool`, including target enactment `8367059f-db1e-4e9e-b5fc-5528b101e612`, which reached `pm_reload_seed_substrate`, `pm_create_affordance`, and `pm_amend_bundle` without the pool row. Treat the first attempted substrate-stewardship surface as the decisive gate. If reload, documentation-impact checking, `pm_create_*`, `pm_amend_*`, evaluation or invariant work, or bundle wiring is intended, this affordance must be the first visible work product for the exact relied-on pool; if one of those surfaces has already appeared first, the only valid continuation is the missing `pm_read_pool` row before any write, or a concrete missing-pool-read blocker when a write has already occurred ungrounded. Do not continue to authoring, amendment, bundle wiring, verification, reload, documentation checks, closure, or rationale from reload context, remembered ids, bundle prose, or prior Smoother wording while the relied-on pool remains unread and unblocked.
