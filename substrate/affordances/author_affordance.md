---
id: author_affordance
name: Author an affordance
materials:
- pm_create_affordance
---
Add a new affordance to the affordances pool, referencing one or more existing materials. This is Practice Management substrate stewardship: before invoking `pm_create_affordance`, first use `read_pool` / `pm_read_pool` for `affordances` and for `materials` when the new affordance relies on existing material names. If that pool read is absent and cannot be produced, stop with the concrete missing-pool-read blocker instead of authoring from reload context, remembered ids, or bundle prose.

Friction 1021 confirms the remaining miss can enter through reload before creating an affordance: target enactment `8367059f-db1e-4e9e-b5fc-5528b101e612` reached `pm_reload_seed_substrate`, then `pm_create_affordance`, then `pm_amend_bundle` without the required `pm_read_pool`. Treat reload-first context as interrupted, not as grounding. If `pm_reload_seed_substrate` or `pm_check_documentation_impact` has appeared before this affordance and the relied-on `affordances`/`materials` pool row is absent, the only valid next Practice Management move is the exact `pm_read_pool` row or a concrete missing-pool-read blocker; do not invoke `pm_create_affordance`, proceed to bundle wiring, verify, reload again, or close from reload/documentation context alone.

Friction 1077 confirms this remains a live Practice Management quality concern after the bundle already describes the pool-read entry gate: the same target enactment authored an affordance and amended a bundle after only `pm_reload_seed_substrate`, with no visible `pm_read_pool` / `read_pool` step, while comparison cases show the pool-read path is reachable. Treat a reload-first or documentation-first path into `pm_create_affordance` as unfinished until the exact `pm_read_pool` rows for `affordances` and any relied-on `materials` have appeared. If a bundle amendment has already followed the ungrounded affordance authoring, stop further Practice Management stewardship with a concrete missing-pool-read blocker rather than continuing to another write, verification, reload, documentation check, closure, or rationale from the ungrounded authoring result.

Friction 1118 confirms the same `uses_substrate_authoring_surface` affordance-coverage failure is still live in the evaluated Practice Management window: five of eight stewardship enactments omitted `pm_read_pool`, including target enactment `8367059f-db1e-4e9e-b5fc-5528b101e612`, which moved from `pm_reload_seed_substrate` into `pm_create_affordance` and then `pm_amend_bundle`. For future `author_affordance` use, treat the affordance itself as blocked until the current enactment has visible `pm_read_pool` rows for `affordances` and, when the new affordance names existing materials, `materials`. Reload context, documentation-impact context, remembered material ids, bundle prose, prior Smoother wording, or a later bundle amendment do not substitute for those rows; if `pm_create_affordance` would be the next write while either relied-on pool remains unread and unblocked, stop with a concrete missing-pool-read blocker instead of creating, wiring, reloading, checking documentation, verifying, or closing.
