---
id: author_affordance
name: Author an affordance
materials:
- pm_create_affordance
---
Author a new affordance only after the current enactment has visibly grounded the substrate surface it will change. Before invoking `pm_create_affordance`, the first Practice Management stewardship work product must be `pm_read_pool` for `affordances`, and also `pm_read_pool` for `materials` when the new affordance relies on existing or newly named material ids/content. A bundle description, reload result, documentation-impact context, remembered ids, prior Smoother wording, or the created affordance result itself is not a substitute for those pool rows.

If `pm_reload_seed_substrate`, `pm_check_documentation_impact`, or another stewardship surface has already appeared before the relevant pool read, the immediate next visible move must be the missing `pm_read_pool` row before any create. If `pm_create_affordance` has already appeared without those rows, the stewardship path is interrupted and unfinished: stop with a concrete missing-pool-read blocker naming the absent pool row(s). Do not continue from an ungrounded `pm_create_affordance` into `pm_amend_bundle`, verification, reload, documentation checks, another write, closure, or rationale.

Friction 1181 confirms this remains a Practice Management quality-affordance-coverage failure in the evaluated window: target enactment `8367059f-db1e-4e9e-b5fc-5528b101e612` reached `pm_reload_seed_substrate`, then `pm_create_affordance`, then `pm_amend_bundle` with no `pm_read_pool`, while other recent misses reached reload or documentation-impact surfaces without the pool row and nearby contrast enactments showed `pm_read_pool` was reachable. Treat `pm_create_affordance` without prior `pm_read_pool` for `affordances` and any relied-on `materials` as a terminal interruption for that stewardship path. The only valid continuation after the ungrounded create is a concrete missing-pool-read blocker; do not wire the created affordance into a bundle, run verification, reload, check documentation impact, continue authoring, close, or explain from the ungrounded create result.

Friction 1195 confirms the same failure is still live after the bundle description and selected rule surfaces already name the entry gate: in target enactment `8367059f-db1e-4e9e-b5fc-5528b101e612`, Practice Management moved from `pm_reload_seed_substrate` to `pm_create_affordance` and then `pm_amend_bundle` without any `pm_read_pool`. For this affordance, make the missing pool row the state of the work itself. If `pm_create_affordance` is the next intended move and the current enactment has not already read `affordances` and any relied-on `materials`, the only permitted next row is the missing `pm_read_pool` or a concrete unreadable-pool blocker. If `pm_create_affordance` has already occurred ungrounded, do not try to repair the pass by wiring, verifying, reloading, documenting, or rationalizing from the created id; stop with a concrete missing-pool-read blocker naming `affordances` and any relied-on `materials`, and leave any later stewardship to a fresh pass that starts from `pm_read_pool`.
