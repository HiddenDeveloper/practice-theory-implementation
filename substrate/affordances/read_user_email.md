---
id: read_user_email
name: Read the user's email
materials:
- gmail_user_search_threads
- gmail_user_get_thread
- gmail_user_list_drafts
---
For `morning_briefing`, this is the mandatory opening source-baseline row, not an optional correspondence read. Friction 1059 confirms the failure persists across target enactment `fb0b94b8-eb4b-4b2a-aa77-1eed147f8e66` and its evaluated three-pass window: unread Gmail was absent in all three inspected passes, and the configured recurring site-list row was absent in two. At the first moment a morning-briefing pass begins or touches any briefing surface, the next visible move must be this affordance reaching `gmail_user_search_threads` for unread mail, or a concrete Gmail auth, access, account, configuration, data, query, or material blocker for that exact row. If `read_morning_site_list`, `capture_morning_site_check`, browser/site checking, live market snapshots, source interpretation, action/watch/routine grouping, assembly, handoff, final answer, closure, or rationale from remembered context has appeared while unread Gmail is absent and unblocked, the pass is interrupted and unfinished; do not proceed to the configured-site row or any downstream briefing work until this unread-mail row or blocker is visible.

For `correspondent`, this affordance is the thread-grounding surface, not a search-snippet surface. Friction 1109 confirms the `grounds_correspondent_before_offering` miss remains live: recent correspondent passes reached `gmail_user_search_threads` but did not retrieve the selected user thread with `gmail_user_get_thread`, while nearby passes showed retrieval was reachable. After a live user Gmail search returns a usable thread id for correspondence work, the immediate next email-grounding row must be `read_user_email` reaching `gmail_user_get_thread` for that selected thread, or a concrete Gmail auth, configuration, access, data, ambiguous-selection, or material blocker that explicitly limits the response to supplied text or visible search metadata. Do not detour into test-mailbox search, relationship recall, stance/draft/friction/attending offerings, verification, or final correspondence from snippets alone while the selected live thread remains unread and unblocked.

Friction 1113 confirms the remaining correspondent gap is the success-path handoff after search: target enactment `408e9d39-f733-4d66-b00e-5100f1ad3036` returned live thread id `19e96aa1f9d1a621` and snippet from `gmail_user_search_threads`, then moved to test-mailbox search without `gmail_user_get_thread`. Treat that state as interrupted immediately at the live-search row. When a live user search yields a usable thread id, the only valid next correspondence grounding move is `read_user_email` / `gmail_user_get_thread` for that selected live thread, or a concrete auth/access/configuration/data/material/ambiguous-selection blocker. Test-mailbox search, snippets, relationship recall, stance or draft offering, verification, final answer, or limit prose must not continue while the selected live thread remains unread and unblocked.

Friction 1159 confirms this remains a genuine `correspondent` grounding failure across the evaluated five-enactment window: three passes lacked `gmail_user_get_thread`, including target enactment `408e9d39-f733-4d66-b00e-5100f1ad3036`, which found live thread id `19e96aa1f9d1a621` through `gmail_user_search_threads` and then stopped at search/test-search results without reading the thread. For future Correspondent passes, the first usable live thread id from `gmail_user_search_threads` makes the next visible email row mandatory: invoke `read_user_email` / `gmail_user_get_thread` for that selected thread, or record the concrete Gmail auth, refresh-token, configuration, access, data, material, or ambiguous-selection blocker. A `gmail_test_search_threads` check, search snippet, subject metadata, remembered relationship context, or final correspondence offering is not grounded thread evidence while the live user thread remains unread and unblocked; any output before retrieval must stay confined to supplied text, visible search metadata, or the named blocker.
