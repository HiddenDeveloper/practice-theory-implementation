---
id: read_user_email
name: Read the user's email
materials:
- gmail_user_search_threads
- gmail_user_get_thread
- gmail_user_list_drafts
---
Read unread mail from the user's Gmail as the first recurring source row for practices that depend on current correspondence context. In `morning_briefing`, this affordance reaching `gmail_user_search_threads` is the non-skippable opening baseline before configured site-list reads, browser/site checks, live market snapshots, source interpretation, assembly, summary, or final answer. If Gmail cannot be read, record the concrete auth, access, data, configuration, or material blocker for unread mail and keep any briefing limited by that blocker; do not treat the configured site list, browser checks, remembered morning context, invalid URL handling, or periodic intent as a substitute for this unread-email row.

Friction 985 confirms the practice-quality stall remains live across the evaluated three-run `morning_briefing` window for signal `reads_morning_sources_before_assembling`: every evaluated enactment lacked `gmail_user_search_threads`, and two also lacked `read_morning_briefing_sites`. For future morning briefings, selecting any downstream morning source or briefing surface while unread email is absent is an interrupted opening state. The immediate next visible move must be this `read_user_email` / `gmail_user_search_threads` row, or a concrete Gmail auth, access, data, configuration, or material blocker; do not continue to configured site-list work, browser checks, live snapshots, source interpretation, assembly, handoff, final answer, closure, or summary from site-list-only or site-check-only context while the unread-mail row remains unread and unblocked.

Friction 988 confirms the correspondent `grounds_correspondent_before_offering` quality concern remains live when a Gmail user search returns a reachable thread id but the pass stops or shifts to test-search/substitute context without `gmail_user_get_thread`. In `correspondent`, a successful `gmail_user_search_threads` result is only an index row: the immediate next correspondence-grounding move must be this same `read_user_email` affordance reaching `gmail_user_get_thread` for the selected live thread, or a concrete credential, OAuth, account, access, data, selection, or material blocker that limits the response to supplied text or visible search metadata. Do not proceed from search-only, test-search-only, snippet-only, or auth-error context into correspondence analysis, stance invitations, draft language, final answers, closure, or any completion-shaped offer while the selected live thread remains unread and unblocked.

Friction 1000 confirms the same correspondent gap remains in target enactment `408e9d39-f733-4d66-b00e-5100f1ad3036`: usable live and test Gmail searches returned thread ids, but the pass closed without `gmail_user_get_thread`. In `correspondent`, if a live user search has produced a reachable thread id, a later test-mailbox search or snippet comparison is not a recovery path and must not be followed by offering, analysis, drafting, stance invitation, final answer, or closure while the live thread remains unread. The next visible move must be this `read_user_email` affordance reaching `gmail_user_get_thread` for the selected live user thread, or a concrete retrieval blocker naming the exact credential, OAuth, account, access, data, selection, or material condition; otherwise confine any output to supplied text or visible search metadata and name the retrieval limit.
