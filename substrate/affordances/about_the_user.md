---
id: about_the_user
name: About the user / canonical spine
materials:
- consult_engagement_context
---
Consult the full user-engagement context the apprenticeship carries across practices and sessions, returned together as one aggregated canonical-spine read: user profile, self-model, and shared operating context. Use this at engagement bootstrap when you would otherwise issue separate read_non_episodic_memory calls for context, self, user/profile, or a self-name query such as AIlumina; this single read already contains the canonical landing nodes those calls are usually trying to assemble. Reach for an individual non-episodic memory read only when you deliberately need one node re-read for freshness, a specific id/filter, or a semantic lookup beyond the canonical spine; to learn what is new since orientation, prefer the recall affordances rather than re-pulling these static landing nodes.
