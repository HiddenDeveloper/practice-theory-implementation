---
id: und_substrate_authoring
name: Authoring the substrate well
---
Friction 725 confirms the Practice Management quality concern remains genuine after another evaluated window: five of eight recent Practice Management enactments still reached reload, documentation-check, authoring, amendment, or bundle-wiring surfaces without a visible same-enactment `pm_read_pool`, while a nearby control case showed the pool-read pattern was reachable. For future Practice Management passes, treat the substrate pool read as the first visible move of any substrate-authoring path. Before reload, documentation-impact checking, `pm_create_*`, `pm_amend_*`, invariant/evaluation work, or `pm_amend_bundle`, stop and name the exact pool whose ids or current content will be relied on; if that pool has not already been exposed in the current enactment, invoke `read_pool` / `pm_read_pool` immediately and only then continue. If the pass cannot name or reach that pool surface, record a concrete substrate-surface blocker and do not proceed from the bundle description, remembered ids, prior trail, or reload/documentation context alone.

Friction 736 confirms the same gate still fails across another evaluated window, including five of eight recent Practice Management enactments that reached reload, documentation-impact, authoring, or bundle-amendment surfaces without `pm_read_pool`. Therefore the gate is not merely descriptive context: it is the first work product of substrate stewardship. At the start of any Practice Management path that may reload, check documentation impact, create, amend, author or amend invariants/evaluations, or wire a bundle, produce a visible pool-grounding row before the affected work: name the exact pool (`teleo_affective`, `understanding`, `rules`, `affordances`, or `materials`) and read it with `pm_read_pool`. If more than one pool supplies ids or content for the intended change, read each relied-on pool before the corresponding write. A bundle description, remembered id, prior Smoother friction content, or reload result is never enough to substitute for this row; when the row cannot be produced, the correct Practice Management outcome is a concrete substrate-surface blocker and no stewardship write.

Friction 755 confirms the remaining Practice Management failure is not lack of description but failure to make the gate the next move after stewardship has already begun. If a Practice Management pass has reached `pm_reload_seed_substrate`, `pm_check_documentation_impact`, any `pm_create_*`, any `pm_amend_*`, evaluation or invariant work, or `pm_amend_bundle` before the relevant `pm_read_pool` row, treat the pass as interrupted and unfinished. The next visible Practice Management move must be the exact `pm_read_pool` for the pool whose ids or current content the work is relying on, or a concrete substrate-surface blocker naming why that pool cannot be read; do not continue to another reload, documentation check, authoring/amendment, bundle wiring, verification, or closure while the relied-on pool is absent and unblocked.

Friction 766 confirms the failure still appears in evaluated Practice Management enactments even after the bundle description names the gate: five of eight passes omitted `pm_read_pool`, and the most recent missing pass moved from `pm_reload_seed_substrate` into `pm_create_affordance` and `pm_amend_bundle` without exposing the relevant pool. Treat this as an unfinished stewardship state at the moment reload, documentation-impact checking, authoring, amendment, evaluation/invariant work, or bundle wiring appears before the relied-on pool read. The immediate next visible Practice Management move must be `pm_read_pool` for the exact pool supplying the ids or current content for the attempted work, or a concrete substrate-surface blocker naming why that pool cannot be read. Do not proceed from reload into `pm_create_*`, `pm_amend_*`, `pm_amend_bundle`, verification, or closure while that pool remains unread and unblocked; nearby passing traces show the pool-read path is reachable, so silence is not acceptable variation.

Friction 793 confirms the gate still fails as a Practice Management quality concern despite the bundle description already naming `pm_read_pool` as the entry condition: five of eight evaluated enactments again reached reload, documentation-impact, authoring, or bundle-wiring surfaces without the required pool read. Treat the pool-grounding row as the first work product of any substrate stewardship pass, not as explanatory prose to remember later. If `pm_reload_seed_substrate`, `pm_check_documentation_impact`, any `pm_create_*`, any `pm_amend_*`, evaluation or invariant work, or `pm_amend_bundle` appears before the exact `pm_read_pool` for the pool supplying the ids or current content, the pass is interrupted; the immediate next visible move must be that `pm_read_pool` or a concrete substrate-surface blocker, and no stewardship write, verification, reload, documentation check, bundle wiring, or closure should proceed while the relied-on pool remains unread and unblocked.

Friction 807 confirms this remains a genuine Practice Management practice-quality failure after the current bundle description already made `read_pool` / `pm_read_pool` the entry gate: five of eight evaluated enactments still omitted the pool row, including paths that moved from reload into authoring, amendment, or documentation-impact work. Treat an absent `pm_read_pool` before any substrate stewardship surface as an immediate stop condition, not as a reminder for later prose. The first visible work product must be the exact pool read for the ids or current content being relied on; if `pm_reload_seed_substrate`, `pm_check_documentation_impact`, `pm_create_*`, `pm_amend_*`, evaluation or invariant work, or `pm_amend_bundle` has already appeared first, the only permitted continuation is that exact `pm_read_pool` or a concrete substrate-surface blocker. Do not proceed to another stewardship action, verification, bundle wiring, or closure from reload context, documentation context, remembered ids, prior Friction content, or bundle description while the relied-on pool remains unread and unblocked.

Friction 853 confirms the continuing failure is a practice-quality drift in the first work product itself: five of eight evaluated Practice Management enactments still lacked `pm_read_pool`, including a target path that went from `pm_reload_seed_substrate` into `pm_create_affordance` and `pm_amend_bundle`. For future Practice Management passes, do not treat a bundle description, prior Smoother wording, remembered ids, reload context, or documentation-impact context as a substitute for a visible pool row. The opening stewardship move must be `pm_read_pool` for the exact pool whose ids or current content will support the work; if any reload, documentation-impact, authoring, amendment, invariant/evaluation work, or bundle wiring has already occurred first, the pass is interrupted and the only permitted continuation is that exact pool read or a concrete substrate-surface blocker. No further stewardship write, verification, reload, documentation check, bundle wiring, or closure should proceed while the relied-on pool remains unread and unblocked.
