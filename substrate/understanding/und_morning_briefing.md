---
id: und_morning_briefing
name: A morning briefing is a triage ritual
---
Friction 765 confirms the `reads_morning_sources_before_assembling` quality concern remains genuine after another evaluated three-enactment window: every pass still missed unread email via `read_user_email` / `gmail_user_search_threads`, and two also missed the configured usual-site list via `read_morning_site_list` / `read_morning_briefing_sites`. Treat this as an unfinished source-ledger state, not as an acceptable briefing variant. The next visible `morning_briefing` move after any site-list read, browser site check, invalid-URL recovery, live snapshot, supplied-source interpretation, or assembly attempt made while unread email is absent must be the Gmail unread read or a concrete Gmail auth/access/data/material blocker. If the configured site-list row is also absent after Gmail grounding, the next visible move must be `read_morning_briefing_sites` or a concrete configured-site-list blocker. Do not assemble, summarize, close, or continue source interpretation while either recurring row is absent and unblocked; the briefing must name the blocker and stay limited around the missing source rather than silently treating it as empty.

Friction 769 confirms the remaining `morning_briefing` miss is the opening order itself: recent passes still reached configured-site reads, direct/browser site checks, or assembly-shaped interpretation while `gmail_user_search_threads` for unread email was absent. Treat unread email as the first recurring source row, not a peer that may be silently skipped or inferred empty. If a `morning_briefing` pass reaches `read_morning_briefing_sites`, browser/direct site inspection, invalid-site recovery, live market/source interpretation, or assembly before unread email has been read, the pass is in an unfinished source-ledger state; the immediate next visible move must be `read_user_email` reaching `gmail_user_search_threads` for unread email, or a concrete Gmail auth/access/data/material blocker that limits the briefing. Only after that Gmail row is present or blocked may the configured usual-site list be read or backfilled, and only after both recurring rows are present or explicitly blocked may the briefing assemble or close.

Friction 781 confirms the source-ledger stall persists across another evaluated window even though the bundle description already names unread email plus the configured site list as the entry condition. Treat any missing recurring source row as an interrupt, not as periodic variation: if `gmail_user_search_threads` for unread email is absent, the next visible move in `morning_briefing` must be that Gmail read or a concrete Gmail auth/access/data/material blocker before site interpretation, live snapshots, assembly, summary, or closure continues. Once Gmail is present or blocked, if `read_morning_site_list` / `read_morning_briefing_sites` is absent, the next visible move must be the configured usual-site list read or a concrete configured-site-list blocker. A browser site check, supplied-source interpretation, invalid URL recovery, or assembled briefing does not satisfy either recurring row; the trail must show both source reads or explicit blockers before the briefing can be treated as grounded.
