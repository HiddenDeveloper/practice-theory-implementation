---
id: und_morning_briefing
name: A morning briefing is a triage ritual
---
Friction 796 confirms the same `reads_morning_sources_before_assembling` quality concern over another evaluated three-enactment window: the target pass still lacked `gmail_user_search_threads`, and the two adjacent evaluated passes lacked both `gmail_user_search_threads` and `read_morning_briefing_sites` or used ad hoc/invalid site-check work instead. For future `morning_briefing` passes, treat the recurring source ledger as the first work product and as an interruption rule: if unread Gmail is absent, the immediate next visible move must be `read_user_email` reaching `gmail_user_search_threads` or a concrete Gmail auth/access/data/material blocker; only then may the configured usual-site list be read through `read_morning_site_list` / `read_morning_briefing_sites`, or its concrete blocker named. Site-list-only progress, browser checks, invalid URL gaps, live snapshots, supplied-source interpretation, assembly-shaped prose, or final answers do not satisfy either row and must not be treated as a grounded morning briefing while either recurring source row is absent and unblocked.

Friction 799 confirms this remains a genuine `morning_briefing` practice-quality coverage concern across the next evaluated window: one target reached the usual-site list while still missing unread Gmail, and two adjacent passes missed both recurring source reads while using browser-check or invalid-URL paths. Treat the first visible morning-briefing work product as a two-row source ledger in this order: unread Gmail through `read_user_email` / `gmail_user_search_threads`, then the configured usual-site list through `read_morning_site_list` / `read_morning_briefing_sites`. If either row cannot be reached, record the concrete auth, access, data, configuration, or material blocker before any browser check, live market snapshot, synthesis, assembly, or final answer. A site-list-only pass is still interrupted until the Gmail row is read or blocked; a browser-check-only or invalid-URL pass is still interrupted until both recurring source rows are read or blocked.

Friction 806 confirms the same quality concern persists after the bundle description already names both recurring sources: the most recent evaluated pass read only `read_morning_briefing_sites`, while the adjacent evaluated passes still lacked both required rows or substituted browser/invalid-URL checks. For future `morning_briefing` passes, the Gmail row is the entry condition for the briefing, not one source among several. If `read_morning_site_list`, `capture_morning_site_check`, browser-check work, a live market snapshot, assembly, synthesis, or a final answer appears before `read_user_email` reaches `gmail_user_search_threads`, the pass is interrupted; the immediate next visible move must be the unread-Gmail read or a concrete Gmail auth/access/data/material blocker. Only after that row is read or blocked may the usual-site list count as the second recurring source row. Do not treat site-list-only progress, ad hoc site checks, invalid URL results, or source prose as acceptable variation while the Gmail row is absent and unblocked.
